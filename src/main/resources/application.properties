# Database Configuration
db.url=jdbc:mysql://localhost:3306/bagstore_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
db.username=root
db.password=210506
db.driver=com.mysql.cj.jdbc.Driver

# Application Configuration
app.name=BagStore
app.version=1.0.0
app.session.timeout=30

# File Upload Configuration
upload.max.size=10485760
upload.temp.dir=/tmp/bagstore-uploads
